# 日常操作指导（手册）

> 本手册面向日常使用：所有命令都在**终端**里执行。
> 打开终端：桌面右键 → Open Terminal，或按 `Ctrl + Alt + T`。
> ROS Humble 基础环境已配置为开终端自动加载，无需手动操作。

---

## 一、环境加载（每次用之前先看这里）

| 要做什么 | 先敲这个命令 |
|----------|--------------|
| 学 MoveIt 教程 / 跑教程示例 | `ws_tutorials` |
| 玩珞石 SR5（仿真或真机） | `ws_rokae` |
| 只用 ROS 基础命令 / Panda 演示 | 什么都不用敲（自动加载） |

说明：
- 这两个快捷命令只对**执行它的那个终端**生效，一个终端加载一个即可
- 新终端才有效；已开着的旧终端先敲 `source ~/.bashrc`
- 如果命令没反应，检查 D 盘是否已挂载（文件管理器里点一下 D 盘）

---

## 二、场景 1：学习 MoveIt 官方教程

```bash
# 第 1 步：加载教程工作空间
ws_tutorials

# 第 2 步：启动对应课程（选一个跑）
ros2 launch moveit2_tutorials move_group_interface_tutorial.launch.py   # 第2课：第一个C++程序 ⭐
ros2 launch moveit2_tutorials planning_scene_tutorial.launch.py          # 规划场景
ros2 launch moveit2_tutorials mtc_tutorial.launch.py                     # 抓取放置
```

- 教程配套文档：`notes/` 目录下有笔记，在线版 https://moveit.picknik.ai/humble/doc/tutorials/tutorials.html
- 课程源码位置：`/media/administrator/D/Projects/MoveItTutorials/src/moveit2_tutorials/doc/`

## 三、场景 2：珞石 SR5 仿真（不连真机，安全）

```bash
# 第 1 步：加载 Rokae 工作空间
ws_rokae

# 第 2 步：启动 SR5 仿真演示（RViz 会自动弹出）
ros2 launch rokae_hardware rokae_moveit_launch.py robot_type:=SR5 use_fake_hardware:=true
```

RViz 里操作：拖动末端彩色标记 → 左侧 Planning 标签 → **Plan** → **Execute**

## 四、场景 3：连接 SR5 真机 ⚠️

**前提检查（每次连真机前确认）：**
1. 电脑网线连到机器人控制器，且同网段（默认控制器 IP：`192.168.21.10`）
2. 控制器固件 ≥ v3.2.1
3. 机器人处于可外部控制状态（示教器上确认）
4. 机器人周围无障碍物、急停按钮在手边

```bash
ws_rokae
# 把 <本机IP> 换成你这台电脑在 192.168.21.x 网段的地址
ros2 launch rokae_hardware rokae_moveit_launch.py robot_type:=SR5 \
  use_fake_hardware:=false robot_ip:=192.168.21.10 local_ip:=<本机IP>
```

**真机安全守则：**
- 第一次执行 Plan 后**先别点 Execute**，看清楚规划的轨迹再执行
- 执行时手放急停按钮上
- 有任何异常立即急停

## 五、场景 4：Panda 演示（MoveIt 自带，最轻量）

```bash
ros2 launch moveit_resources_panda_moveit_config demo.launch.py
```

## 六、停止与清理（重要！）

| 操作 | 方法 |
|------|------|
| 停止当前终端里启动的程序 | 在该终端按 `Ctrl + C` |
| 切换不同演示前 | **必须先停干净**，否则会互相污染（同名节点冲突） |
| 检查还有哪些节点在跑 | `ros2 node list` |
| 一键清理所有残留 ROS 节点 | 见下方命令 |

一键清理命令（切换演示前、或感觉状态混乱时用）：
```bash
pkill -f "move_[g]roup|ros2_contr[o]l_node|rv[i]z2|robot_state_p[u]blisher|sp[a]wner.*controller|demo.la[u]nch"
```
> 注意方括号写法是故意的（防止 pkill 误杀当前终端自己）。

## 七、防止电脑卡死的规矩 💻

这台机器 7.7GB 内存，以下情况**会卡死**，务必避免：

1. ❌ 编译时同时开着 RViz / 机器人节点
2. ❌ 编译不限速（满 10 核 + 大 C++ 文件吃光内存）
3. ❌ 同时跑多个演示

**正确姿势：**
```bash
# 编译前先停掉所有 ROS 程序（用上面的一键清理），然后：
MAKEFLAGS="-j4" colcon build --symlink-install --executor sequential
```

## 八、RViz 使用技巧

- **保存配置用 `Ctrl + S`**（Save Config），不要用 "Save Config As"（那个对话框很卡，原因见 notes/04）
- 自己的配置文件：`~/.rviz/my_moveit.rviz`
- 用指定配置启动：`rviz2 -d ~/.rviz/my_moveit.rviz`
- RViz 卡顿时：缩小窗口 / 先停掉再重开

## 九、常用排查命令

```bash
ros2 node list                  # 当前运行的节点
ros2 topic list                 # 当前话题
ros2 topic echo /joint_states   # 看关节状态数据
ros2 topic hz /joint_states     # 看发布频率
ulimit -Sl                      # 应显示 unlimited（memlock 权限，重启后生效）
```

## 十、更新这个 GitHub 仓库

```bash
cd /media/administrator/D/Projects/RosLearning
git add -A
git commit -m "写清楚改了什么"
git push                        # 凭证已配好，直接推
```

## 十一、出了问题怎么办

1. 先看 `notes/04-踩坑记录.md`（7 个已知坑都有解法）
2. 报错含 `bad_alloc` / `Resource temporarily unavailable` → memlock 权限问题，重启电脑
3. 报错含 `panda_joint` / 节点名冲突 → 用第六节的一键清理后重启
4. RViz 打不开 → 检查 `echo $DISPLAY` 是否为 `:1`
